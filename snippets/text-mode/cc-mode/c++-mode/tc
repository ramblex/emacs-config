#name : topcoder
# --
#include <algorithm>
#include <fstream>
#include <iostream>
#include <list>
#include <map>
#include <math.h>
#include <set>
#include <sstream>
#include <stack>
#include <string>
#include <vector>

using namespace std;

#define FOR(i, a, b) for (unsigned int i = a; i < b; ++i)
#define REP(i, N) for (unsigned int i = 0; i < N; ++i)

#define pb push_back
#define ALL(V) V.begin(), V.end()
#define SIZE(A) A.size()

typedef vector<int> VI;

class ${1:`(file-name-sans-ext)`}
{
public:
$> $0
private:
};

int main()
{
  $1 *s = new $1;
}
